@startuml
skinparam handwritten true
start
:Customer logs in;
:Customer browses available foods;
:Customer creates order with items;
:System validates item availability;
if (Valid order?) then (Yes)
  :System creates Order and OrderItems;
  :System calculates total price;
  :System sends notification to restaurant staff;
  :Order status = pending;
  :Staff reviews order;
  if (Approve order?) then (Yes)
    :Status = approved;
    :Notify customer "order approved";
    :Kitchen/packing process;
    :Status updates (preparing/on the way/delivered);
    :Notify customer on updates;
  else (No)
    :Status = cancelled;
    :Notify customer "order rejected";
  endif
else (No)
  :Return validation error;
endif
stop
@enduml
